# TypeScript MCP Server Development

Reference for building MCP servers using the official TypeScript SDK.

## Instructions

- Use the **@modelcontextprotocol/sdk** npm package: `npm install @modelcontextprotocol/sdk`
- Import from specific paths: `@modelcontextprotocol/sdk/server/mcp.js`, `@modelcontextprotocol/sdk/server/stdio.js`
- Use `McpServer` class for high-level server implementation
- Use **zod** for input/output schema validation: `npm install zod@3`
- Always provide `title` field for tools, resources, and prompts
- Use `registerTool()`, `registerResource()`, and `registerPrompt()` methods
- Return both `content` (for display) and `structuredContent` (for structured data) from tools
- For local integrations, use `StdioServerTransport` for stdio-based communication
- Test servers with MCP Inspector: `npx @modelcontextprotocol/inspector`

## See Also

- [references/server-setup.md](references/server-setup.md) - Server setup patterns
- [references/tools.md](references/tools.md) - Tool implementation patterns
